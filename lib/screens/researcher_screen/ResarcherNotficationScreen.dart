import 'package:flutter/material.dart';

class ResarcherNotficationScreen extends StatelessWidget {
  const ResarcherNotficationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [
          Center(child: Text('Notfication'),)
        ],
      ),
    );
  }
}
